---
const { href = "#contact", label = "Записаться на урок" } = Astro.props;
---

<a href={href} class="shine-button">{label}</a>

<style>
  .shine-button {
    display: block;
    margin-inline: auto;

    padding: 15px 30px;
    font-size: 16px;
    color: white;
    background: linear-gradient(135deg, #ff7809, #ef521e);

    text-decoration: none;
    max-width: max-content;
    font-weight: 600;

    border: none;
    border-radius: 30px;
    cursor: pointer;

    animation: pulse 2s infinite;
    transition: transform 0.2s ease;
  }

  .shine-button:hover {
    transform: scale(1.05);
  }

  @keyframes pulse {
    0%,
    100% {
      box-shadow: 0 0 10px #ff7809;
    }
    50% {
      box-shadow: 0 0 20px #ee470f;
    }
  }
</style>
